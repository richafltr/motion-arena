services:
  motion-arena:
    build: .
    ports:
      - "5173:5173"
    environment:
      PORT: 5173
      MOMASK_API_URL: ${MOMASK_API_URL:-}
      MOTION_ARENA_MODE: ${MOTION_ARENA_MODE:-momask}
